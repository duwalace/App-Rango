rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRAS ABERTAS PARA TESTE - PERMITIR TUDO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}